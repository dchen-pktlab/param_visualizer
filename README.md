# TODO
- [ ] Ability to filter for domains (lots of telemetry garbage gets captured by default)
- [ ] Improvement to the convert-to-url-request box

# Building
```
./gradlew jar
```
The file will be at `/build/libs/param-collector.jar`
